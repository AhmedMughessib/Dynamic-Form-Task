import './App.css';

import DynamicForm from './componenets/dynamicForm';

function App() {
  return (
    <div className="App">
      <DynamicForm />
    </div>
  );
}

export default App;

// give me an exmale of dynamic react form (for example some inputs will be added, delted. or changeg based on a speceifc input). also, vlaidata the inputs using yup dynamicaly (the validation rules will change bases on this same input)
